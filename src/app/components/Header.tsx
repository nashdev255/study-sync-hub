import { AppBar, Toolbar, Typography, Button } from '@mui/material';

const Header = () => {
  return (
    <header>
      <AppBar>
        <Toolbar>
          <Typography></Typography>
          <Button></Button>
          <Button></Button>
        </Toolbar>
      </AppBar>
    </header>
  );
};

export default Header;
